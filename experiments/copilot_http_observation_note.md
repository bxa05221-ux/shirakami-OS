# Live Copilot HTTP observation

The live observation target is:

Copilot SDK -> Shirakami HTTP /v1/execute -> RuntimeResult -> Evidence -> Trace -> AIwitness.

Provider credentials remain outside Shirakami semantic state. Human Gate remains required.

This document records the intended observation boundary; the live run is only marked verified after the complete chain is observed.