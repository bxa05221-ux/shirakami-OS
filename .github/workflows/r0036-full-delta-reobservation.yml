name: R0036 Full Delta Re-observation

on:
  pull_request:
    paths:
      - "runtime/full_delta_reobservation.py"
      - "runtime/delta_reobservation.py"
      - "runtime/evidence_replay.py"
      - "tests/test_r0036_full_delta_reobservation.py"
      - ".github/workflows/r0036-full-delta-reobservation.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install pytest
      - run: python -m pytest tests/test_r0036_full_delta_reobservation.py -q
