verification_id: V-01-independent-reproduction-workflow
name: Independent reproduction workflow
version: "1.0"
date: "2026-09-17"
branch: docs/verification-v1.0
status: PARTIAL
scope:
  - GitHub Actions workflow definition
  - Python version matrix
  - Quickstart execution command
  - Runtime test execution command
  - Structured verification artifact generation
implemented:
  path: .github/workflows/independent-reproduction.yml
  commit: 852d11176d47c1a73fcc66aa29ed070659d753c0
  triggers:
    - workflow_dispatch
    - push to docs/verification-v1.0
  python_versions:
    - "3.11"
    - "3.12"
  dependency:
    - "pytest==8.4.2"
  artifact_generation:
    - environment metadata
    - quickstart.log
    - runtime-tests.xml
    - runtime-tests.log
    - adversarial-tests.xml
    - adversarial-tests.log
  commands:
    - python examples/quickstart/run.py
    - python -m pytest -q --junitxml=../verification-artifacts/runtime-tests.xml
    - python -m pytest -q test_adversarial_inputs.py --junitxml=../verification-artifacts/adversarial-tests.xml
observed:
  workflow_file_created: true
  workflow_file_retrieved_and_confirmed: true
  artifact_generation_configured: true
  workflow_execution: NOT_VERIFIED
limitations:
  - The updated workflow configuration has not yet been confirmed through a dedicated completed run.
  - Verification is intended to run on GitHub-hosted Ubuntu runners, not an independently managed local or third-party environment.
  - The workflow installs pytest from the public package index, introducing network and package-distribution dependencies.
  - Artifact presence, test counts, and detailed step logs require confirmation from an actual workflow run.
quality_gate:
  decision: HOLD_PENDING_GATES
  next_action: Execute or manually dispatch the dedicated workflow, retrieve run metadata, jobs, logs, and artifacts, then update this record with observed results.
reviewer: human review pending
