# R0055 — Shirakami OS β1.0 Formal Handoff

Status: `implementation_handoff`

## Purpose

Record the formal boundary between α exploration and β1.0 operation.

This artifact records the research-side handoff received as `的目yaml` and translates no new theory. Runtime-side implementation must treat unresolved theoretical questions as unresolved and return them to the research side.

## Handoff Identity

```yaml
id: shirakami-os-beta-1.0-handoff
title: "Shirakami OS β1.0 — Landscape Continuity Runtime"
version: "1.0"
status: "implementation_handoff"
```

## Phase Boundary

```text
α
探す段階
  ↓
R0055
正式引継ぎ
  ↓
β1.0
動かす段階
```

Principle:

> 探す段階から、動かす段階へ

## β1.0 Architecture

```text
Landscape
    ↓
Evidence
    ↓
Protocol
    ↓
Runtime
    ↓
Adapter
    ↓
Backend
```

The β1.0 handoff identifies the following verified areas:

- Landscape: state transition, continuity, multi-account, multi-protocol
- Evidence: capture, lineage, addressability, temporal addressability, temporal selection
- Protocol: input, IR, lineage, interpretation boundary, interpretation provenance
- Runtime: execution, HTTP Landscape pipeline, applicability, runtime/boundary tests
- Adapter: adapter exchange
- Development: canonical runtime verification, protocol artifact trigger, `test-runtime`

## Repository State

Canonical CI:

```text
Workflow: Runtime β0.1 Verification
Job/check: test-runtime
Protocol trigger: protocols/**
```

The canonical CI document confirms that `test-runtime` is the current required-check candidate and that experiment-specific workflows are not to be made required merely because they exist. See `docs/architecture/CANONICAL_CI.md`.

Main branch protection remains explicitly unconfigured:

```yaml
protection: "not_configured"
rulesets: 0
```

Therefore:

> β1.0 Runtime implementation completion and operational repository protection are separate states.

## Verification Rules

The following rules are preserved as operational constraints:

- One Change
- One Verification
- ファイルの存在と実験成功を混同しない
- 確認できないものを成功としない
- Evidenceを後から書き換えない
- Artifact mismatchを推測で解消しない

## External AI Boundary

External AI may participate in:

- Observation
- Architecture Review
- Implementation
- Experiment

Authority remains:

```yaml
authority: none
```

External AI is not a truth authority for Landscape, Evidence, or Protocol semantics.

## Theoretical Boundary

Runtime implementation must not create new theory.

```text
Formal 的目yaml
      ↓
Implementation
      ↓
Observed implementation issue
      ↓
Research-side feedback
```

A theoretical issue discovered during implementation is not to be silently resolved inside Runtime.

## β1.0 Definition

β1.0 is not the completion of a particular AI model or vendor Adapter.

It is the state in which external AI, Runtime, Protocol, and Adapter may be exchanged while Landscape and Evidence continuity can continue to be observed and verified.

GPT Adapter operation is therefore one implementation/verification path, not the definition of β1.0 itself.

## Next Operational Verification

The first practical β1.0 operation is Adapter Exchange.

The verification target is not identical AI responses. The target is continuity and observability:

- Landscape is preserved
- Protocol is preserved
- Evidence remains addressable and unbroken where expected
- Adapter exchange does not move domain meaning into the Runtime boundary
- Differences before/after exchange remain observable

The known boundary:

```text
Landscape snapshot reconstruction
        ≠
Evidence lineage reconstruction
```

must remain a verification target rather than being silently treated as a defect to be repaired.

## R0055 Result

R0055 formally records:

```text
α exploration: handed off
β1.0 implementation state: recorded
main protection: not configured
β1.0 operation: next
```

## Next Step

```text
R0055
  ↓
Human-side GitHub main protection configuration
  ↓
β1.0 operation start
  ↓
Adapter Exchange verification
  ↓
Renderer Exchange verification
  ↓
Only then add boundaries made necessary by actual operation
```
