# R0017 — Execution Note

This experiment intentionally stays inside the existing Runtime → Evidence → Projection → Landscape path.

The test does not introduce a replay mechanism. It simply applies already-captured Evidence records through the existing Projection function to independent Landscape states and compares the resulting observable state.

The comparison is operational only: equality of snapshots is not interpreted as semantic identity or continuity.
