# R0023 — Execution Command

```bash
SHIRAKAMI_LIVE_GITHUB_TEST=1 SHIRAKAMI_GITHUB_TOKEN="$TOKEN" python -m runtime.operational_github_read
```

This command is read-only. The credential is supplied by the process environment and is not persisted.
