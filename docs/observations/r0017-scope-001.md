# R0017 — Scope Boundary

R0017 is intentionally narrower than a replay or migration feature.

It asks only whether the existing Projection operation yields an observable, repeatable Landscape snapshot when given the same ordered Evidence.

Any interpretation beyond that observation remains outside the Runtime boundary.
