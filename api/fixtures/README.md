# Protocol fixtures

This directory contains source-derived protocol fixtures used for API integration tests.

`shirakami-model-v3.2.yaml` is a test fixture copied from the public `shirakami-model` repository. It is not a replacement for the source protocol and is used only to verify that the Shirakami OS API can accept and preserve the protocol's identity and selected structure.
